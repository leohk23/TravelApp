# Secrets. The ODPT token is used only by tools/ at build time and must never
# reach the repo or the browser.
.env
.env.local

# Trip exports dropped here for debugging. They carry booking confirmation
# numbers, and this repo is public.
trip.json
*.trip.json
